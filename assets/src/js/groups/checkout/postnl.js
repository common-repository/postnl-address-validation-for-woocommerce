jQuery(document).ready(function () {

	postnl_address_validation.init();
	/* Loader UI */
	postnl_loader.init();
	/* Address selector */
	postnl_address_select.init();
	/* Notifications */
	postnl_address_validation_notifications.init();
	/* Field toggle */
	postnl_field_toggle.init();
});
